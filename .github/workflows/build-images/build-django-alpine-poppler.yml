name: Docker build django:alpine-poppler

on: workflow_call

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Docker build
        uses: tiwariav/github-actions/docker-build@v1
        with:
          context: src
          file: ./src/images/django/alpine/Dockerfile.d.poppler
          image: tiwariav/django
          suffix: alpine-poppler
